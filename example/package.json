{
  "private": true,
  "name": "example",
  "version": "1.0.0",
  "scripts": {
    "build": "rimraf dist && cross-env A=1 B=2 C=3 D=4 pnpm exec webpack",
    "test": "node dist/main.js"
  },
  "license": "MIT",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "rimraf": "^3.0.2",
    "webpack": "^5.72.1",
    "webpack-cli": "^4.9.2"
  }
}
